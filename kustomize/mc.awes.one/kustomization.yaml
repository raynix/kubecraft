apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  - ../base

configMapGenerator:
  - name: kubecraft
    files:
      - server.properties

patchesStrategicMerge:
  - patch.yaml

commonAnnotations:
  domain: mc.awes.one
